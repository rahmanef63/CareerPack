# Frontend Next.js — pakai Convex self-hosted prod (example.convex.cloud).
# NEXT_PUBLIC_* diinline time `next build`, jadi WAJIB set sebelum build.
NEXT_PUBLIC_CONVEX_URL=https://example.convex.cloud
