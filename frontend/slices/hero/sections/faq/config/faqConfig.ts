/**
 * Presentation/behavior config for the FAQ section — no copy lives here,
 * only structural + animation numbers and the support contact target.
 */

/** Support inbox — same address used by the authenticated Help slice. */
export const FAQ_SUPPORT_EMAIL = "support@careerpack.org";
export const FAQ_SUPPORT_HREF = `mailto:${FAQ_SUPPORT_EMAIL}?subject=CareerPack%20Support`;

/** Base scroll-reveal stagger step (seconds) applied to left-column blocks. */
export const FAQ_REVEAL_STEP_SECONDS = 0.1;
